import styled from "styled-components";

export const ProductListStyled = styled.ul`
    width: 92%;
    max-width: 1140px;
    height: max-content;

    margin-inline: auto;
    padding: 1rem;

    display: flex;
    gap: 1rem;

    overflow-x: auto;

`